# Optional HTTPS endpoint accepting JSON { name, email, message, website }.
# Leave empty to open the visitor's email app with their completed message.
# Never put private service keys in VITE_ variables: they are public.
VITE_CONTACT_ENDPOINT=
